version: '3.8'

services:
  api:
    build: .
    container_name: stl-prediction-service
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=prod
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
